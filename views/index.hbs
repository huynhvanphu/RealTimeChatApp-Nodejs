<body>
    <div class="container-fluid h-100">
        <div class="row justify-content-center h-100" id="chatZoom">
            {{> videoStream}}

            {{> login}}

            {{> roomList}}
            
            {{> onlineList}}
        
            {{> chatBox}}

            {{> chatZoomBox}}

        </div>
    </div>
</body>